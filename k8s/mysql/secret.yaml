apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: ticket-booking
type: Opaque
stringData:
  # WARNING: Change these default passwords before deploying to production!
  # Use strong, randomly generated passwords or external secret management
  MYSQL_ROOT_PASSWORD: "password"
  MYSQL_PASSWORD: "password"
